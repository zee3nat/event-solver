[project]
name = "event-solver"
authors = ["Event Solver Team"]
description = "A decentralized platform for solving and organizing complex event logistics on the blockchain"
telemetry = false
requirements = []
[contracts.event-solver]
path = "contracts/event-solver.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
